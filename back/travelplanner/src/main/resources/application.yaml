spring:
  profiles:
    active:
      - local
    group:
      local:
        - mysql
        - mybatis
        - secret

---

spring:
  config:
    activate:
      on-profile: mysql
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/travel_planner?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul
    username: root
    password: 1234

---

spring:
  config:
    activate:
      on-profile: mybatis
mybatis:
  config-location: classpath:/mybatis-config.xml
  mapper-locations: classpath:/mapper/*.xml

---

spring:
  config:
    activate:
      on-profile: secret
secret:
  key: NMA8JPctFuna59f5Lkjfwlief19802374183479871923JHFENMA8JPctFuna59f5Lkjfwlief19802374183479871923JHFE